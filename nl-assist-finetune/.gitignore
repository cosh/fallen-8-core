# Reproducible artifacts - never committed (spec FT-5). The generator, trainer config,
# Modelfile.template and eval harness ARE committed; the produced dataset, weights, and
# per-run outputs below are not.
eval/results/
dataset/
merged/
adapter*/
*.gguf

# Generated per run from the committed templates/config (one PROVENANCE.<model>.md per variant).
Modelfile
PROVENANCE*.md

# Toolchain working dirs.
train/.venv/
llama.cpp/
__pycache__/

# FL-3: dropped-in feedback captures to consolidate (the operator's downloaded files).
# The consolidation output lands in dataset/ (already ignored above).
feedback/inbox/

# Optional local `pip freeze` snapshot (machine/platform-specific, e.g. +cuXXX torch). The
# exact working versions are already pinned in train/requirements.txt, so this is just a
# personal reference, never committed.
train/requirements.lock
