{
  "$comment": "LoRA fine-tune configuration for the phi4-f8 variant: the FULL Phi-4 (14B, MIT) base (feature delegate-model-variants). Same dataset + pipeline as train-config.phi4-f8-mini.json; only the base and the hardware-scaled knobs differ. Reproducible per spec FT-1. Produce it with: VARIANT=phi4-f8 ./run.sh all",

  "$hardwareNote": "14B 4-bit QLoRA does NOT fit the 8GB GPU the mini trained on. Budget ~16GB+ VRAM (RTX 3090/4090 class). On OOM: keep perDeviceBatchSize 1 and drop maxSeqLength (e.g. 1536), then raise gradAccumSteps to hold the effective batch. Inference of the 14B is GPU-only in practice.",

  "baseModel": "microsoft/phi-4",
  "baseModelLicense": "MIT",
  "seed": 20260719,
  "maxSeqLength": 2048,

  "$targetModulesNote": "VERIFY against the actual module names of microsoft/phi-4 before training - the Phi-3/Phi-4 family often exposes FUSED projections ('qkv_proj', 'gate_up_proj') rather than the split names below. 'python train_lora.py --inspect --config train/train-config.phi4-f8.json' prints the model; set targetModules to the names it actually has, or LoRA attaches to nothing.",
  "lora": {
    "r": 16,
    "alpha": 32,
    "dropout": 0.05,
    "targetModules": ["q_proj", "k_proj", "v_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]
  },

  "quantize": {
    "load4bit": true,
    "bnbQuantType": "nf4",
    "computeDtype": "bfloat16",
    "useDoubleQuant": true
  },

  "training": {
    "$scaleNote": "Scaled for 14B on ~16-24GB VRAM: batch 1 with more accumulation holds a comparable effective batch to the mini's 2x8 while fitting memory; gradient checkpointing is mandatory here.",
    "epochs": 2,
    "perDeviceBatchSize": 1,
    "gradAccumSteps": 16,
    "learningRate": 0.0002,
    "lrScheduler": "cosine",
    "warmupRatio": 0.03,
    "weightDecay": 0.0,
    "gradientCheckpointing": true,
    "optim": "paged_adamw_8bit",
    "loggingSteps": 5
  },

  "$responseTemplateNote": "MUST match how Phi-4's tokenizer opens an assistant turn - Phi-4 has used '<|im_start|>assistant<|im_sep|>', which differs from Phi-4-mini's '<|assistant|>'. VERIFY with 'python train_lora.py --inspect --config train/train-config.phi4-f8.json' before spending GPU hours; a wrong marker silently trains on the whole prompt.",
  "responseTemplate": "<|im_start|>assistant<|im_sep|>",

  "output": {
    "ggufQuant": "Q4_K_M",
    "ollamaModel": "phi4-f8"
  }
}
