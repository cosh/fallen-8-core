# Credential mount for the integrations sidecar (feature integrations): one file per credential,
# bind-mounted read only at /run/secrets. This .gitignore ships so the path exists after a clone;
# nothing written here is ever committed.
*
!.gitignore
